import { describe, expect, it } from "vitest";
import { useNotification } from "./notification";

describe("notification", () => {
  it("should work", () => {
    // expect(useNotification()).toEqual("notification");
  });
});
